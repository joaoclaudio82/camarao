# Variáveis de ambiente (opcional)
# Nenhuma API key necessária para o modo OpenCV puro

# Porta do servidor (padrão: 8000)
PORT=8000

# Tamanho máximo de arquivo (bytes) — padrão 100 MB
MAX_FILE_SIZE=104857600
